# Todo


What I would like to do for this project is have some firmware loaded onto the STM32 and then in an app that can run on the PC have a controler panel that will allow us to communicate with the STM32 (allow us to change a config setting, or read current state values). I am thinking something like BetaFlight, initialy we can just get the serial communications working, but eventually it would be nice if the firmware binary could be downloaded and flashed to the STM32 through the app using DFO.

We need to build a plan that would allow us to achive this, but I am unsure as to what language to build the app in, would be nice if it was a web app using bootstrap or similar, maybe java, but I have no real preference. interview me with options you think would work well for this scenario, we will start with compatability with Linux (unbuntu) but it would be nice if the app could run on windows and apple in the future.

Lets brainstorm some ideas untill we get a basic project settled


